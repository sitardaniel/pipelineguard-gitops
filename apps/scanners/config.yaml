apiVersion: v1
kind: ConfigMap
metadata:
  name: scanner-config
  namespace: pipelineguard
data:
  # Target repositories to scan
  TARGET_REPOS: |
    https://github.com/sitardaniel/pipelineguard-app
    https://github.com/sitardaniel/pipelineguard-infra
  # Scan output directory
  OUTPUT_DIR: /results
  # Severity threshold
  SEVERITY_THRESHOLD: MEDIUM
  # Notification settings - editable from the config-ui web interface
  NOTIFY_SLACK_ENABLED: "true"
  NOTIFY_EMAIL_ENABLED: "false"
  NOTIFY_EMAIL_TO: ""
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scan-results-pvc
  namespace: pipelineguard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
